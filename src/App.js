import { CarForm, CarSearch, CarList, CarValue } from './components';

const App = () => {
  return (
    <div>
      <CarForm />
      <CarSearch />
      <CarList />
      <CarValue />
    </div>
  );
};

export default App;
